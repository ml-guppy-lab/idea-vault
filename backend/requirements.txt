# FastAPI core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0            # async PostgreSQL driver
greenlet>=3.0.0            # required by SQLAlchemy async engine
motor>=3.4.0               # async MongoDB driver
redis>=5.0.0               # async Redis client (redis.asyncio)

# Pydantic & settings
pydantic>=2.0.0
pydantic-settings>=2.0.0
pydantic[email]            # EmailStr support

# Auth & security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0               # password hashing (replaces passlib)
python-multipart>=0.0.9     # required for OAuth2PasswordRequestForm
authlib>=1.3.0              # OAuth 2.0 / OpenID Connect client (Google login)
httpx>=0.27.0               # async HTTP — required by authlib's async integration
itsdangerous>=2.0.0         # session signing — required by SessionMiddleware for OAuth state
