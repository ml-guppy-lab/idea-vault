# TODO: Implement ideas CRUD using MongoDB (Motor) in the ideas epic.
# This file is a placeholder — all routes will be rewritten to use get_mongo_db().

from fastapi import APIRouter

router = APIRouter(prefix="/ideas", tags=["ideas"])
